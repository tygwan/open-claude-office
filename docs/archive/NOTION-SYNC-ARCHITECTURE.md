# Notion 양방향 동기화 아키텍처

개발 전주기 문서와 프로젝트 관리 데이터를 Notion과 양방향 동기화하여, **작성 = 확인**이 되는 구조를 설계합니다.

---

## 핵심 원칙

### 기존 문제

```
기존 흐름 (2단계 변환):
Claude Code → PROGRESS.md (마크다운) → 파서 → JSON → 대시보드
                       ↑
                 불필요한 중간 레이어
                 마크다운은 사람이 읽기 위한 포맷이지
                 기계가 소비하기 위한 포맷이 아님
```

### 목표 흐름

```
목표 흐름 (변환 없음):
Claude Code ←──MCP──→ Notion DB ←── 브라우저
                         │
                    작성 = 확인
                    변환 레이어 없음
```

### Source of Truth 전략

```
코드와 밀접한 것 → 로컬 파일 (Source of Truth)
프로젝트 관리 → Notion (Source of Truth)

로컬에 남는 것:                     Notion으로 가는 것:
─────────────────                  ──────────────────
CLAUDE.md (세션 컨텍스트)            프로젝트 현황 (Projects DB)
.claude/settings.json              태스크 관리 (Tasks DB)
.claude/agents/                    Stage Gate 기록 (Gates DB)
.claude/skills/                    PRD, TECH-SPEC (Documents)
.claude/hooks/                     Sprint 데이터 (Sprints DB)
소스 코드                           기술 부채 (Tech Debt DB)
테스트 코드                         피드백/학습 (Feedback DB)
.env, config 파일                   활동 로그
```

---

## Notion DB 설계

### 전체 구조

```
Notion Workspace
│
├── 📊 Projects DB          ← 메인 대시보드 (전 프로젝트 한눈에)
├── 📋 Tasks DB              ← 전 프로젝트 태스크 통합
├── 🚪 Gates DB              ← Stage 전환 기록
├── 📄 Documents DB          ← PRD, TECH-SPEC 등 문서
├── 📈 Sprints DB            ← Sprint 관리
├── 🔧 Tech Debt DB          ← 기술 부채 추적
├── 💬 Feedback DB            ← 학습, ADR, Retro
└── 📝 Activity Log DB       ← 활동 이력
```

### 1. Projects DB (메인 대시보드)

프로젝트 전체 현황을 한눈에 보는 중심 DB.

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Name         │ Title      │ 프로젝트 이름                    │
│ Stage        │ Select     │ MVP / PoC / Production          │
│ Progress     │ Number     │ 전체 진행률 (0-100)              │
│ Health       │ Select     │ 🟢 Good / 🟡 Warning / 🔴 Critical │
│ Priority     │ Select     │ High / Medium / Low              │
│ Repo URL     │ URL        │ GitHub 저장소 링크               │
│ Local Path   │ Text       │ 로컬 경로                        │
│ Sprint       │ Relation   │ → Sprints DB (현재 Sprint)       │
│ Tech Stack   │ Multi-select│ React, Node, Python 등          │
│ Test Coverage│ Number     │ 테스트 커버리지 %                 │
│ CI Status    │ Select     │ Passing / Failing / None         │
│ Last Activity│ Date       │ 마지막 활동 일시                  │
│ Deploy Target│ Select     │ Local / Staging / Production     │
│ Description  │ Text       │ 프로젝트 한 줄 설명               │
│ Created      │ Created time│ 생성일                          │
│ Updated      │ Last edited │ 수정일                          │
└──────────────┴────────────┴─────────────────────────────────┘

Views:
- Table: 전체 프로젝트 목록
- Board: Stage별 칸반 (MVP | PoC | Production)
- Gallery: 프로젝트 카드 뷰
- Timeline: 프로젝트 타임라인
```

### 2. Tasks DB (태스크 관리)

전 프로젝트의 태스크를 통합 관리.

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Task         │ Title      │ 태스크 이름                      │
│ Project      │ Relation   │ → Projects DB                   │
│ Phase        │ Select     │ Phase 1 / Phase 2 / ...         │
│ Status       │ Select     │ Backlog/Todo/In Progress/Done/Blocked │
│ Priority     │ Select     │ P0 / P1 / P2                    │
│ Assignee     │ Person     │ 담당자                           │
│ Sprint       │ Relation   │ → Sprints DB                    │
│ Story Points │ Number     │ 스토리 포인트                     │
│ Due Date     │ Date       │ 마감일                           │
│ GitHub Issue │ URL        │ 연결된 GitHub Issue               │
│ GitHub PR    │ URL        │ 연결된 Pull Request               │
│ Tags         │ Multi-select│ feature / bug / tech-debt / docs │
│ Notes        │ Text       │ 메모                             │
│ Created      │ Created time│                                 │
│ Completed At │ Date       │ 완료 일시                        │
└──────────────┴────────────┴─────────────────────────────────┘

Views:
- Table: 전체 태스크
- Board: Status별 칸반
- Board: Project별 칸반
- Board: Phase별 칸반
- Calendar: 마감일 기준
- Filter: 내 태스크만 / 현재 Sprint만 / Blocked만
```

### 3. Gates DB (Quality Gate 통합 기록)

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Gate         │ Title      │ MVP Gate / PoC Gate / pre-build │
│              │            │ / pre-deploy / pre-commit       │
│              │            │ / pre-merge / pre-release       │
│              │            │ / post-release                  │
│ Category     │ Select     │ Stage / Pipeline / Dev          │
│ Project      │ Relation   │ → Projects DB                   │
│ From Stage   │ Select     │ MVP / PoC (Stage gates only)    │
│ To Stage     │ Select     │ PoC / Production (Stage only)   │
│ Status       │ Select     │ Not Checked/Passed/Failed/Blocked│
│ Checked At   │ Date       │ 검증 일시                        │
│ Passed At    │ Date       │ 통과 일시                        │
│ Blocker Count│ Number     │ 미충족 필수 항목 수               │
│ Warning Count│ Number     │ 미충족 권장 항목 수               │
└──────────────┴────────────┴─────────────────────────────────┘

각 Gate 행의 페이지 본문:
- Required 체크리스트 (체크박스)
- Recommended 체크리스트 (체크박스)
- 검증 결과 상세 내용
```

### 4. Documents DB (문서 관리)

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Document     │ Title      │ 문서 이름                        │
│ Project      │ Relation   │ → Projects DB                   │
│ Type         │ Select     │ PRD/TECH-SPEC/DISCOVERY/RUNBOOK/│
│              │            │ ADR/SLA/SECURITY/API-SPEC/      │
│              │            │ ARCHITECTURE/NARRATIVE/          │
│              │            │ STACK-PROFILE/EXPERIENCE-BLOCKS/ │
│              │            │ DESIGN-PROFILE/CONTENT-JSON/    │
│              │            │ TOKENS-CSS/VALIDATION-REPORT/   │
│              │            │ SUMMARY/CHANGELOG/SPRINT-RETRO  │
│ Status       │ Select     │ Draft / Review / Final / Archived│
│ Stage        │ Select     │ MVP / PoC / Production          │
│ Version      │ Text       │ v1.0, v1.1 등                   │
│ Last Editor  │ Person     │ 마지막 편집자                     │
│ Updated      │ Last edited│ 수정일                          │
└──────────────┴────────────┴─────────────────────────────────┘

각 행의 페이지 본문 = 실제 문서 내용 (PRD, TECH-SPEC 등)

Views:
- Table: 전체 문서
- Board: Status별 (Draft | Review | Final)
- Board: Type별
- Filter: Project별
```

### 5. Sprints DB

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Sprint       │ Title      │ Sprint N                        │
│ Project      │ Relation   │ → Projects DB                   │
│ Status       │ Select     │ Planning/Active/Completed/Cancelled│
│ Period       │ Date range │ 시작일 ~ 종료일                   │
│ Goal         │ Text       │ Sprint 목표                      │
│ Velocity     │ Number     │ 완료 포인트                      │
│ Planned Pts  │ Number     │ 계획 포인트                      │
│ Tasks        │ Relation   │ → Tasks DB (Sprint 태스크들)     │
│ Burndown     │ Text       │ 번다운 데이터 (JSON)              │
└──────────────┴────────────┴─────────────────────────────────┘

각 행의 페이지 본문:
- Sprint 목표 상세
- 번다운 차트 (이미지 또는 텍스트)
- Daily Standup 로그
- Retrospective (Sprint 완료 시)
```

### 6. Tech Debt DB

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Item         │ Title      │ 부채 항목                        │
│ Project      │ Relation   │ → Projects DB                   │
│ Severity     │ Select     │ Critical / High / Medium / Low  │
│ Category     │ Select     │ Code/Architecture/Testing/Docs/ │
│              │            │ Infrastructure/Security         │
│ Status       │ Select     │ Open / In Progress / Resolved   │
│ Source       │ Text       │ 파일 경로 또는 영역              │
│ Effort       │ Select     │ S / M / L / XL                  │
│ Created Stage│ Select     │ MVP / PoC / Production          │
│ Task         │ Relation   │ → Tasks DB (해결 태스크 연결)     │
└──────────────┴────────────┴─────────────────────────────────┘

Views:
- Table: 전체 부채
- Board: Severity별
- Board: Category별
- Filter: Critical + Open만
```

### 7. Feedback DB

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Item         │ Title      │ 피드백 제목                      │
│ Project      │ Relation   │ → Projects DB                   │
│ Type         │ Select     │ Learning/ADR/Retro/User-Feedback│
│ Category     │ Multi-select│ bugs/performance/security/      │
│              │            │ architecture/tooling/process    │
│ Source       │ Select     │ Fix Commit/Arch Change/Sprint   │
│              │            │ End/User Report/Post-mortem     │
│ Impact       │ Select     │ High / Medium / Low             │
│ Action Taken │ Checkbox   │ 행동에 반영했는지                 │
│ Created      │ Created time│                                 │
└──────────────┴────────────┴─────────────────────────────────┘

각 행의 페이지 본문:
- Learning: Problem → Root Cause → Learning → Application
- ADR: Context → Options → Decision → Consequences
- Retro: Keep → Problem → Try → Action Items
- User Feedback: 원본 피드백 + 분석 + 대응 계획
```

### 8. Activity Log DB

```
Schema:
┌──────────────┬────────────┬─────────────────────────────────┐
│ Property     │ Type       │ Description                     │
├──────────────┼────────────┼─────────────────────────────────┤
│ Activity     │ Title      │ 활동 요약                        │
│ Project      │ Relation   │ → Projects DB                   │
│ Type         │ Select     │ Commit/PR/Deploy/Gate/Sprint/   │
│              │            │ Incident/Release/StateTransition│
│              │            │ /QualityGate/PipelinePhase/     │
│              │            │ CLIFallback                     │
│ Detail       │ Text       │ 상세 내용                        │
│ Link         │ URL        │ GitHub 링크 등                   │
│ Timestamp    │ Date       │ 발생 시각                        │
│ Actor        │ Text       │ 실행자 (사용자 또는 Claude)       │
└──────────────┴────────────┴─────────────────────────────────┘
```

---

## DB 간 관계 (Relation Map)

```
Projects DB (중심)
    │
    ├──→ Tasks DB          (1:N — 프로젝트의 모든 태스크)
    │       │
    │       └──→ Sprints DB (N:N — 태스크가 속한 Sprint)
    │
    ├──→ Gates DB          (1:N — 프로젝트의 Gate 기록들)
    │
    ├──→ Documents DB      (1:N — 프로젝트의 모든 문서)
    │
    ├──→ Sprints DB        (1:N — 프로젝트의 Sprint들)
    │
    ├──→ Tech Debt DB      (1:N — 프로젝트의 기술 부채)
    │
    ├──→ Feedback DB       (1:N — 프로젝트의 피드백/학습)
    │
    └──→ Activity Log DB   (1:N — 프로젝트의 활동 이력)
```

---

## 양방향 동기화 시나리오

### 시나리오 1: Claude Code → Notion (개발 중 자동 동기화)

```
개발자가 /feature start "add-auth" --phase 2 실행
    │
    ▼
Claude Code:
    ├── git checkout -b feature/add-auth
    ├── Notion Tasks DB에 행 추가:
    │   { Task: "Add auth", Project: "open-pantheon",
    │     Phase: "Phase 2", Status: "In Progress", Priority: "P0" }
    ├── Notion Activity Log에 기록:
    │   { Activity: "Feature started: add-auth", Type: "Commit" }
    └── Notion Projects DB progress 업데이트
```

```
개발자가 /feature complete 실행
    │
    ▼
Claude Code:
    ├── quality-gate 실행
    ├── Notion Tasks DB에서 해당 행 Status → "Done"
    ├── Notion Tasks DB에서 Completed At 기록
    ├── Notion Projects DB progress 재계산
    ├── Notion Sprints DB velocity 업데이트
    ├── Notion Activity Log에 기록:
    │   { Activity: "Feature completed: add-auth", Type: "PR" }
    └── PR 생성 → Notion Tasks DB에 GitHub PR URL 추가
```

```
/lifecycle promote poc 실행
    │
    ▼
Claude Code:
    ├── Gate 검증 실행
    ├── Notion Gates DB에 행 추가:
    │   { Gate: "MVP Gate", Status: "Passed", Checked At: now }
    │   페이지 본문: required 항목 체크 결과
    ├── Notion Projects DB Stage → "PoC"
    ├── Notion Documents DB에 Gate Report 문서 추가
    └── Notion Activity Log에 기록:
        { Activity: "Stage promoted: MVP → PoC", Type: "Gate" }
```

### 시나리오 2: Notion → Claude Code (외부 변경 감지)

```
PM이 Notion Tasks DB에서 새 태스크 추가:
  { Task: "Add rate limiting", Priority: "P0", Phase: "Phase 2" }
    │
    ▼
Claude Code 세션 시작 시 (/sync 또는 자동):
    ├── Notion Tasks DB 쿼리: Status = "Backlog" or "Todo"
    ├── 새 태스크 감지
    └── 출력: "새 태스크가 추가되었습니다:
              - Add rate limiting (P0, Phase 2)
              작업을 시작할까요?"
```

```
PM이 Notion Tasks DB에서 우선순위 변경:
  "Add logging" P1 → P0
    │
    ▼
Claude Code 다음 세션에서:
    ├── Notion Tasks DB 쿼리
    ├── 변경 감지
    └── 출력: "우선순위가 변경되었습니다:
              - Add logging: P1 → P0
              Sprint 백로그를 재정렬할까요?"
```

```
PM이 Notion Documents DB에서 PRD 수정:
  PRD의 Requirements 섹션에 새 요구사항 추가
    │
    ▼
Claude Code에서 /sync 실행:
    ├── Notion Documents DB에서 PRD 페이지 fetch
    ├── 로컬 PRD 캐시와 diff 비교
    ├── 변경 감지
    └── 출력: "PRD가 업데이트되었습니다:
              - 새 요구사항 추가: FR-008 Rate Limiting
              TECH-SPEC과 TASKS에 반영할까요?"
```

### 시나리오 3: 양방향 충돌 처리

```
충돌 상황:
  Claude Code에서 Task status → "Done" 변경 (로컬)
  동시에 PM이 Notion에서 같은 Task priority 변경

해결 전략:
  ├── 서로 다른 필드 → 양쪽 모두 적용 (자동 병합)
  ├── 같은 필드 → Notion 우선 (사람의 판단 존중)
  └── 충돌 발생 시 → 사용자에게 알림 후 선택 요청

충돌 해결 우선순위:
  1. Status 변경: Claude Code 우선 (코드 상태를 정확히 아는 쪽)
  2. Priority 변경: Notion 우선 (PM 판단)
  3. Content 변경: Notion 우선 (사람이 직접 편집한 것)
  4. 그 외: 최신 timestamp 우선
```

---

## 동기화 메커니즘

### Hook 기반 자동 동기화

```
기존 hook 확장:

phase-progress.sh (PostToolUse)
  기존: TASKS.md 변경 → PROGRESS.md 업데이트
  추가: → Notion Tasks DB Status 업데이트
       → Notion Projects DB Progress 업데이트

auto-doc-sync.sh (PostToolUse)
  기존: git commit → CHANGELOG 업데이트
  추가: → Notion Activity Log에 기록
       → Notion Projects DB Last Activity 업데이트

새 hook:

notion-sync.sh (PostToolUse)
  트리거: Write/Edit on docs/ 디렉토리
  동작: 변경된 문서를 Notion Documents DB에 동기화
```

### 동기화 명령어

```bash
# 전체 동기화 (양방향)
/notion sync

# 로컬 → Notion (푸시)
/notion push
/notion push --project open-pantheon
/notion push --tasks-only
/notion push --docs-only

# Notion → 로컬 (풀)
/notion pull
/notion pull --project open-pantheon
/notion pull --changes-only

# 상태 확인
/notion status                  # 동기화 상태, 마지막 동기화 시간
/notion diff                    # 로컬과 Notion 차이점

# 초기 설정
/notion init                    # DB 구조 생성 + 기존 데이터 마이그레이션
/notion init --db-only          # DB 구조만 생성
/notion init --migrate          # 기존 마크다운 → Notion 마이그레이션
```

### 동기화 주기

```
실시간 (hook 기반):
  - Task status 변경 → 즉시 Notion 반영
  - Activity log → 즉시 기록
  - Progress 업데이트 → 즉시 반영

세션 단위:
  - 세션 시작 시 → Notion에서 변경사항 pull
  - 세션 종료 시 → 미동기 항목 push

수동:
  - /notion sync → 전체 양방향 동기화
  - 문서 내용 변경 → 수동 push (빈번한 API 호출 방지)
```

---

## 대시보드 뷰 설계

### Projects DB — 메인 대시보드 뷰

```
Board View (Stage별):

┌─────────────────┬─────────────────┬─────────────────┐
│     🔵 MVP      │     🟡 PoC      │    🟢 Production │
├─────────────────┼─────────────────┼─────────────────┤
│                 │                 │                 │
│ ┌─────────────┐ │ ┌─────────────┐ │ ┌─────────────┐ │
│ │ experiment  │ │ │ portfolio   │ │ │open-pantheon│ │
│ │ ██░░░ 20%   │ │ │ █████░ 51%  │ │ │ ████████ 82%│ │
│ │ 🟢 Active   │ │ │ 🟢 Active   │ │ │ 🟢 Stable   │ │
│ │ Sprint 1    │ │ │ Sprint 3    │ │ │ Sprint 8    │ │
│ └─────────────┘ │ └─────────────┘ │ └─────────────┘ │
│                 │                 │                 │
│ ┌─────────────┐ │ ┌─────────────┐ │                 │
│ │ project-x   │ │ │ resumely    │ │                 │
│ │ █░░░░  8%   │ │ │ ███░░ 34%   │ │                 │
│ │ 🔴 Stale    │ │ │ 🟡 Warning  │ │                 │
│ │ No Sprint   │ │ │ Sprint 2    │ │                 │
│ └─────────────┘ │ └─────────────┘ │                 │
└─────────────────┴─────────────────┴─────────────────┘
```

### Tasks DB — 칸반 뷰

```
Board View (Status별, 현재 프로젝트 필터):

┌──────────┬──────────┬──────────┬──────────┬──────────┐
│ Backlog  │  Todo    │In Progress│  Done   │ Blocked  │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│          │          │          │          │          │
│ Rate     │ API docs │ Add auth │ Setup CI │ DB migr. │
│ limiting │ P1       │ P0       │ P0       │ P1       │
│ P1       │          │ Sprint 3 │ Sprint 3 │ Sprint 3 │
│          │ Logging  │          │          │ blocked  │
│ Cache    │ P1       │ Tests    │ Lint     │ by auth  │
│ layer    │          │ P0       │ setup    │          │
│ P2       │          │ Sprint 3 │ P0       │          │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```

### 프로젝트 상세 페이지

```
Projects DB의 각 행 클릭 시 페이지:

┌─────────────────────────────────────────────────────┐
│ open-pantheon                                        │
│ Stage: 🟢 Production    Health: 🟢 Good              │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Lifecycle Progress                                   │
│ ●━━━━━━━●━━━━━━━●━━━━━━━◐                           │
│ MVP     PoC     Prod    Operating                    │
│                                                      │
│ Phase Progress                                       │
│ Phase 1: ████████████████████ 100% ✅                │
│ Phase 2: ████████████████░░░░  80%                   │
│ Phase 3: ████████░░░░░░░░░░░░  40%                   │
│ Phase 4: ░░░░░░░░░░░░░░░░░░░░   0%                   │
│                                                      │
│ Current Sprint: Sprint 8                             │
│ Velocity: 21 pts | Coverage: 82% | CI: ✅ Passing    │
│                                                      │
│ Gate History                                         │
│ ✅ MVP Gate — 2026-01-15                             │
│ ✅ PoC Gate — 2026-02-10                             │
│                                                      │
│ Tech Debt: 3 items (0 Critical)                      │
│ Open Tasks: 12 | In Progress: 3 | Blocked: 1        │
│                                                      │
│ Recent Activity                                      │
│ • feat: add monitoring dashboard (2h ago)            │
│ • fix: resolve auth timeout #45 (yesterday)          │
│ • PR #44 merged: API rate limiting (2d ago)          │
│                                                      │
│ 📄 Documents                                         │
│ ├── PRD (Final)                                      │
│ ├── TECH-SPEC (Final)                                │
│ ├── API-SPEC (Review)                                │
│ ├── RUNBOOK (Draft)                                  │
│ └── SLA (Draft)                                      │
└─────────────────────────────────────────────────────┘
```

---

## 마이그레이션 전략

### 기존 마크다운 → Notion 마이그레이션

```
Phase 1: DB 구조 생성 (Day 1)
────────────────────────────
/notion init --db-only
  → 8개 DB 생성 (빈 상태)
  → Relation 설정
  → View 생성

Phase 2: 프로젝트 데이터 마이그레이션 (Day 1-2)
────────────────────────────────────────────
/notion init --migrate
  → 각 프로젝트의 settings.json 읽기
     → Projects DB에 행 추가
  → 각 프로젝트의 PROGRESS.md 파싱
     → Progress 수치 추출 → Projects DB 업데이트
  → 각 프로젝트의 phases/*/TASKS.md 파싱
     → Tasks DB에 행 추가
  → 각 프로젝트의 docs/*.md 읽기
     → Documents DB에 페이지 생성 (본문 포함)

Phase 3: Hook 연결 (Day 2-3)
────────────────────────────
기존 hook에 Notion 동기화 로직 추가
  → phase-progress.sh + Notion update
  → auto-doc-sync.sh + Notion log

Phase 4: 마크다운 의존성 제거 (점진적)
────────────────────────────────────
단계적으로 마크다운 → Notion 전환:
  1단계: Tasks (TASKS.md → Tasks DB) — 가장 빈번한 변경
  2단계: Progress (PROGRESS.md → Projects DB formula)
  3단계: Documents (PRD.md 등 → Documents DB pages)
  4단계: Feedback (LEARNINGS.md → Feedback DB)

마크다운 완전 제거는 하지 않음:
  - CLAUDE.md는 로컬 유지 (세션 컨텍스트용)
  - CONTEXT.md는 로컬 유지 (토큰 최적화용)
  - settings.json은 로컬 유지 (Claude Code 설정)
```

---

## Context Optimizer 연동

### Notion 데이터의 컨텍스트 로딩

```
기존 (마크다운):
  Turn 1: CLAUDE.md + CONTEXT.md → ~1.1K tokens
  Turn 2: TASKS.md, SPEC.md → +2-3K tokens

변경 후 (Notion):
  Turn 1: CLAUDE.md + CONTEXT.md (로컬) → ~1.1K tokens
  Turn 2: Notion Tasks DB query (현재 Sprint, In Progress만)
          → 로컬 캐시 저장 → +1-2K tokens
  Turn 3+: Notion Documents DB fetch (필요한 문서만)
          → 로컬 캐시 저장 → on-demand
```

### 로컬 캐시 전략

```
.claude/cache/notion/
├── projects.json          ← Projects DB 스냅샷 (세션 시작 시 갱신)
├── tasks-active.json      ← 현재 Sprint의 In Progress 태스크
├── current-sprint.json    ← 현재 Sprint 정보
└── docs/
    ├── prd-summary.md     ← PRD 요약 (전체 X, 요약만 캐시)
    └── tech-spec-summary.md

캐시 갱신 규칙:
  - 세션 시작: projects.json, tasks-active.json 갱신
  - /notion pull: 전체 캐시 갱신
  - 5분 이내 재접근: 캐시 사용 (API 호출 절약)
  - 문서 본문: 명시적 요청 시에만 fetch
```

---

## 구현 로드맵

```
Step 1: Notion 연결 (필수 선행)
──────────────────────────────
- Notion MCP 토큰 재설정
- 연결 테스트
- 권한 확인 (create, read, update)

Step 2: DB 구조 생성
──────────────────────
- /notion init --db-only 구현
- 8개 DB + Schema + Relations + Views
- 테스트 데이터로 뷰 확인

Step 3: notion-sync 스킬 개발
──────────────────────────────
- /notion push: 로컬 → Notion
- /notion pull: Notion → 로컬 캐시
- /notion sync: 양방향
- /notion status: 상태 확인
- /notion diff: 차이점 표시

Step 4: 기존 워크플로우 연동
──────────────────────────────
- /feature start → Tasks DB 행 추가
- /feature complete → Tasks DB 업데이트 + Activity Log
- /lifecycle promote → Gates DB + Projects DB Stage 변경
- /sprint start/end → Sprints DB

Step 5: Hook 통합
──────────────────
- phase-progress.sh → + Notion update
- auto-doc-sync.sh → + Notion Activity Log
- notion-sync.sh (새 hook) → docs/ 변경 시 Notion Documents 동기화

Step 6: 기존 데이터 마이그레이션
──────────────────────────────────
- /notion init --migrate 구현
- 기존 프로젝트의 마크다운 데이터 → Notion DB
- 검증 후 점진적으로 마크다운 의존성 제거

Step 7: Context Optimizer 연동
──────────────────────────────
- 로컬 캐시 전략 구현
- Notion fetch → 캐시 → context-optimizer 연동
- 토큰 예산 내에서 Notion 데이터 활용
```

---

## 설정 구조

### settings.json 확장

```jsonc
{
  "notion": {
    "enabled": true,
    "sync": {
      "auto_push_on_task_change": true,
      "auto_push_on_commit": true,
      "auto_pull_on_session_start": true,
      "push_documents": false,          // 문서는 수동 push (API 호출 절약)
      "conflict_resolution": {
        "status": "local_wins",         // 코드 상태는 로컬이 정확
        "priority": "notion_wins",      // 우선순위는 PM 판단 존중
        "content": "notion_wins",       // 사람이 편집한 것 존중
        "default": "latest_wins"        // 그 외는 최신 timestamp
      }
    },
    "databases": {
      "projects": "notion-db-id-here",
      "tasks": "notion-db-id-here",
      "gates": "notion-db-id-here",
      "documents": "notion-db-id-here",
      "sprints": "notion-db-id-here",
      "tech_debt": "notion-db-id-here",
      "feedback": "notion-db-id-here",
      "activity_log": "notion-db-id-here"
    },
    "cache": {
      "dir": ".claude/cache/notion",
      "ttl_minutes": 5,
      "auto_refresh_on_session_start": true
    }
  }
}
```
