# Orchestrator Pipeline Dashboard — 계획 문서

> 작성일: 2026-02-27
> 프로젝트: open-claude-office
> 상태: 계획 단계

---

## 1. 프로젝트 비전

### 한 줄 요약

**내 명령 하나가 어떤 흐름으로 실행되고, 어떤 결과를 만들었는지, 살아있는 파이프라인으로 시각화한다.**

### 핵심 가치

Notion이나 외부 SaaS 대시보드 대신, **로컬에서 동작하는 Interactive Pipeline Flow**를 만든다.
각 CLI(Claude, Codex, Gemini)가 만드는 모든 이벤트가 실시간으로 흘러가는 노드 그래프로 보인다.
여러 프로젝트를 하나의 대시보드에서 관리한다. ~/dev/ 폴더의 프로젝트들을 VSCode처럼 좌측 사이드바에서 선택하고, 각 프로젝트의 라이프사이클 단계(MVP/PoC/Production)를 대시보드 UI에서 직접 전환한다.

```
기존 방식:       명령 → 결과 확인 → 수동 기록 → Notion에 정리
목표 방식:       명령 → 파이프라인이 자동 생성 → 노드가 반짝이며 진행 → 완료 시 자동 기록
```

### 왜 만드는가

1. **3개 CLI 동시 운용** — Claude, Codex, Gemini를 역할별로 병렬 사용하지만, 전체 흐름을 한눈에 볼 수 없음
2. **전주기 추적 필요** — MVP → PoC → Production까지 어느 도메인에서 얼마나 진행됐는지 파악해야 함
3. **토큰/비용 최적화** — 각 CLI의 토큰 소모, 컨텍스트 사용률을 모니터링하고 최적 배분해야 함
4. **Notion 불필요** — 중간 변환 레이어 없이, JSONL → 직접 시각화
5. **멀티 프로젝트 관리** — ~/dev/ 아래 다수의 프로젝트를 개별 대시보드 설정 없이 하나의 인터페이스에서 통합 관리. Antigravity/Cursor처럼 IDE 스타일 UX 제공

---

## 2. 현재 인프라 맵

### 2.1 사용 중인 시스템

```
┌─────────────────────────────────────────────────────────────┐
│  개발 환경: Ubuntu (WSL2)                                     │
│  Windows 브라우저에서 localhost 접근 가능 (WSL2 포트 포워딩)     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  CLI 도구                                                     │
│  ├── Claude Code (Opus 4.6)                                  │
│  │   ├── Agent Teams (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1)│
│  │   ├── Subagents (Task tool, .claude/agents/*.md)          │
│  │   ├── MCP 서버 연동                                        │
│  │   ├── Hooks (PreToolUse, PostToolUse)                     │
│  │   ├── /remote-control (폰/태블릿에서 세션 제어)             │
│  │   └── StatusLine (실시간 상태 표시)                         │
│  ├── Codex CLI (OpenAI)                                      │
│  └── Gemini CLI (Google)                                     │
│                                                               │
│  프레임워크                                                    │
│  └── open-claude-office (.claude/ 에이전트/스킬/훅 프레임워크)  │
│      ├── 47 agents, 28 skills, 9 hooks                       │
│      ├── Phase/Sprint 관리 시스템                              │
│      ├── Context Optimizer                                   │
│      ├── Quality Gate (pre-commit, pre-merge, pre-release)   │
│      ├── /craft 파이프라인 (분석→디자인→빌드→검증→배포)         │
│      ├── /craft-preview (SvelteKit localhost:4173 서빙)       │
│      ├── /gh (GitHub CLI 통합 스킬)                           │
│      ├── State Machine (init→analyzing→...→done)             │
│      ├── CLI 분배 (Claude lead, Codex 검증, Gemini 디자인)    │
│      └── docs/ (전주기 문서)                                   │
│          ├── DEVELOPMENT-LIFECYCLE.md (MVP→PoC→Prod 프로세스) │
│          ├── 개발전주기.md (8개 기술 도메인)                    │
│          └── MVP-POC개발전주기.md (단계별 체크리스트)           │
│                                                               │
│  글로벌 설정 (~/.claude/)                                      │
│  ├── settings.json (hooks, env, statusLine)                  │
│  ├── statusline-command.sh (4줄 상태 표시)                    │
│  └── hooks/ (pre-commit-check, pr-doc-linker, post-commit)  │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 CLI 역할 분배 (현재)

| CLI | 주 역할 | 강점 | 비용 특성 |
|-----|---------|------|----------|
| **Claude Code** | 오케스트레이션, 설계, 복잡한 로직, 문서 | Agent Teams, Subagents, Hooks, MCP | 토큰 비용 높음, 기능 최다 |
| **Codex CLI** | 코드 생성, 리팩토링, 파일 탐색, 조사 | 샌드박스(read-only), 저비용 탐색 | 토큰 비용 낮음, 탐색에 최적 |
| **Gemini CLI** | 검증, 분석, 디자인, 시각화 | 대용량 컨텍스트, 자동 수락 | 무료 티어 활용 가능 |

### 2.3 향후 추가 시스템 (3개)

#### stitch-skills (Google Labs)
- **URL**: https://github.com/google-labs-code/stitch-skills
- **역할**: UI 디자인 자동화, React 컴포넌트 생성, 멀티페이지 웹 빌드
- **6개 스킬**: design-md, react-components, stitch-loop, enhance-prompt, remotion, shadcn-ui
- **연동**: `npx skills add google-labs-code/stitch-skills --skill [name] --global`
- **대시보드 활용**: 대시보드 UI 컴포넌트를 stitch-loop로 빠르게 프로토타이핑
- **파이프라인 활용**: enhance-prompt로 사용자 명령을 최적화된 프롬프트로 변환 → 파이프라인 입력 개선

#### remotion (Remotion Dev)
- **URL**: https://github.com/remotion-dev/remotion
- **역할**: React로 프로그래밍 방식 비디오/애니메이션 생성
- **핵심**: JSX/CSS → MP4 렌더링, 프레임 기반 애니메이션, `useCurrentFrame()` + `interpolate()`
- **대시보드 활용**: 파이프라인 흐름을 녹화하여 비디오 리포트 생성
- **전주기 활용**: 개발 과정 타임라인 영상 자동 생성 (스프린트 회고, 릴리스 데모)
- **stitch-skills 연동**: stitch-skills에 이미 remotion 스킬이 포함 → 프로젝트 워크스루 비디오 자동 생성

#### claude-office-skills (tfriedel)
- **URL**: https://github.com/tfriedel/claude-office-skills
- **역할**: Office 문서 자동화 (PPTX, DOCX, XLSX, PDF)
- **핵심 기능**:
  - PPTX: HTML/CSS → PowerPoint 변환, 템플릿 기반 슬라이드 생성
  - DOCX: 변경 추적, OOXML 조작, 텍스트 추출
  - XLSX: 수식 기반 재무 모델, 색상 코딩, 데이터 유효성 검증
  - PDF: 양식 자동 채우기, 문서 병합, 데이터 추출
- **의존성**: LibreOffice (soffice), Poppler (pdftoppm), Pandoc
- **대시보드 활용**: 파이프라인 히스토리를 PPTX 프레젠테이션이나 PDF 리포트로 자동 내보내기
- **전주기 활용**: 스프린트 리포트 PPTX, 기술 설계서 DOCX, 비용 분석 XLSX 자동 생성

#### 3개 시스템의 통합 흐름

```
[파이프라인 이벤트 로그]
         │
         ├──→ stitch-skills    ──→ 대시보드 UI 컴포넌트 빠른 생성
         │    (design + react)      enhance-prompt로 입력 최적화
         │
         ├──→ remotion         ──→ 파이프라인 흐름 비디오 녹화
         │    (React → MP4)         스프린트 회고 영상, 릴리스 데모
         │
         └──→ claude-office    ──→ 리포트 자동 생성
              (PPTX/DOCX/XLSX)     비용 분석, 기술 문서, 프레젠테이션
```

---

## 3. 대시보드 설계

### 3.1 핵심 컨셉: Interactive Pipeline Flow

**정적 패널 대시보드가 아닌, 살아있는 파이프라인 흐름도.**

명령을 내리면 노드가 생성되고, 이벤트가 흘러가며, 진행중인 노드가 천천히 반짝이고 어두워지는 불빛 효과로 흐름을 파악한다. 완료되면 기록으로 남는다.

### 3.1b 멀티 프로젝트 레이아웃 (VSCode-like)

**하나의 프로젝트만 보는 대시보드가 아닌, ~/dev/ 전체 프로젝트를 관리하는 워크스페이스.**

```
┌──────────────────────────────────────────────────────────────────┐
│  ◉ open-claude-office    [project-A] [project-B] [+]    MVP ▼   │
├─────────┬────────────────────────────────────────────────────────┤
│         │                                                        │
│  ~/dev/ │   Pipeline Flow (Active Project)                       │
│         │                                                        │
│  ▼ dev/ │   ┌──────┐    ┌──────┐    ┌──────┐                   │
│    ├ algo│   │🔵 api│───→│🟢code│───→│🟡test│                   │
│    ├ bim │   │ ◉    │    │ ●    │    │ ○    │                   │
│    ├ memo│   └──────┘    └──────┘    └──────┘                   │
│    ├ n8n │                                                       │
│  ★ open │                                                        │
│    ├ fol │                                                        │
│    └ ... │                                                        │
│         ├────────────────────────────────────────────────────────┤
│         │  Command History (collapsible)                         │
│         │  10:30 "auth 구현" ✓  │  10:25 "DB 설계" ✓             │
├─────────┴────────────────────────────────────────────────────────┤
│  🔵 Claude idle │ 🟢 Codex idle │ 🟡 Gemini idle │ Tokens: 12.5k│
└──────────────────────────────────────────────────────────────────┘
```

#### 사이드바 기능
- `~/dev/` 디렉토리 트리 표시 (fs.readdir)
- `.claude/settings.json` 존재 여부로 프로젝트 감지 (★ 아이콘)
- 클릭 시 프로젝트 전환 → 해당 프로젝트의 파이프라인 표시
- 프로젝트별 `lifecycle.current_stage` 읽어 배지 표시

#### 상단 바
- 프로젝트 탭: 브라우저 탭처럼 열린 프로젝트 전환
- 라이프사이클 토글: MVP | PoC | Production (세그먼트 컨트롤)
  - 변경 시 해당 프로젝트의 `.claude/settings.json` → `lifecycle.current_stage` 업데이트
  - 에이전트 동작 수준이 즉시 변경됨 (예: coverage 30% → 80%)

#### 하단 바
- 활성 CLI 상태 (idle/active)
- 세션 토큰 사용량
- 비용 추적

### 3.2 노드 시각 효과

```
상태별 노드 표현:

○ idle       — 어두운 회색, 정지
◐ queued     — 희미한 glow, 대기
● active     — 밝은 pulse 애니메이션 (2초 주기로 밝아졌다 어두워짐)
◉ streaming  — 빠른 pulse (0.8초 주기, CLI 출력 스트리밍 중)
✓ done       — 짧은 flash 후 차분한 색상으로 정착, opacity 50%
✗ error      — 빨간 flash 후 빨간색 유지
```

```css
/* 핵심 CSS 애니메이션 */
@keyframes node-pulse {
  0%   { box-shadow: 0 0 4px  rgba(59,130,246,0.3); opacity: 0.7; }
  50%  { box-shadow: 0 0 20px rgba(59,130,246,0.8); opacity: 1.0; }
  100% { box-shadow: 0 0 4px  rgba(59,130,246,0.3); opacity: 0.7; }
}

.node-active    { animation: node-pulse 2s ease-in-out infinite; }
.node-streaming { animation: node-pulse 0.8s ease-in-out infinite; }
.node-done      { opacity: 0.5; transition: opacity 0.5s; }
```

### 3.3 엣지(연결선) 흐름 효과

```css
@keyframes edge-flow {
  0%   { stroke-dashoffset: 20; }
  100% { stroke-dashoffset: 0; }
}

.edge-active {
  stroke-dasharray: 5 5;
  animation: edge-flow 1s linear infinite;  /* 점선이 흘러가는 효과 */
  stroke: rgba(59,130,246,0.6);
}

.edge-done {
  stroke-dasharray: none;
  stroke: rgba(100,100,100,0.3);
}
```

### 3.4 CLI별 노드 색상

| CLI | 색상 | 의미 |
|-----|------|------|
| Claude | 🔵 Blue (#3B82F6) | 오케스트레이션, 메인 |
| Codex | 🟢 Green (#10B981) | 탐색, 생성 |
| Gemini | 🟡 Amber (#F59E0B) | 검증, 분석 |
| Agent Teams | 🟣 Purple (#8B5CF6) | 팀 협업 |

### 3.5 파이프라인 흐름 예시

```
하나의 명령이 만드는 파이프라인:

┌─ Pipeline: "auth 시스템 구현해" ── 10:30:15 ── in progress ──┐
│                                                                │
│   ┌──────────┐                                                 │
│   │ 나의 명령 │ "auth 시스템 구현해"                              │
│   │ purpose: │ feature                                         │
│   │    ◉     │ (밝은 pulse)                                    │
│   └────┬─────┘                                                 │
│        │                                                       │
│   ┌────┴─────────┬────────────────┬──────────────┐            │
│   ▼              ▼                ▼              ▼            │
│ ┌────────┐  ┌────────┐     ┌────────┐    ┌────────┐         │
│ │🔵Claude│  │🔵Claude│     │🟢Codex │    │🟡Gemini│         │
│ │api-    │  │db-     │     │        │    │        │         │
│ │designer│  │archite │     │        │    │        │         │
│ │  ◉     │  │  ●     │     │  ●     │    │  ○     │         │
│ └──┬─────┘  └──┬─────┘     └──┬─────┘    └──┬─────┘         │
│    │           │              │            │                  │
│    ▼           ▼              ▼            ▼                  │
│ ┌────────┐ ┌────────┐  ┌────────┐  ┌────────┐              │
│ │auth.ts │ │schema  │  │Login   │  │auth    │              │
│ │  ◉→✓  │ │.prisma │  │.tsx    │  │.test.ts│              │
│ │backend │ │database│  │frontend│  │testing │              │
│ │  mvp   │ │  mvp   │  │  mvp   │  │  mvp   │              │
│ └────────┘ └────────┘  └────────┘  └────────┘              │
│                                                                │
│ ◉ = pulse(밝음)  ● = 진행중  ○ = 대기  ✓ = 완료               │
└────────────────────────────────────────────────────────────────┘
```

### 3.6 두 가지 뷰

#### View 1: Pipeline Flow (실시간)
- 현재 진행중인 파이프라인의 노드 그래프
- 노드 pulse 애니메이션으로 활성 상태 표시
- 엣지 flow 애니메이션으로 데이터 흐름 표시
- 각 노드 클릭 시 CLI 출력 실시간 스트리밍 표시

#### View 2: Command History (기록)
- 완료된 파이프라인의 타임라인
- 접기/펼치기로 상세 이벤트 확인
- 명령 → 발생한 이벤트 → 생성된 파일 → 도메인/라이프사이클 매핑
- 누적 Domain Coverage 차트
- MVP Gate 진행률

---

## 4. 기술 아키텍처

### 4.1 3계층 구조

```
┌──────────────────────────────────────────────────────────────┐
│  Layer 1: Capture (이벤트 수집)                                 │
│                                                                │
│  Claude ──→ PostToolUse Hook ──→ events.jsonl                 │
│  Codex  ──→ Wrapper Script   ──→ events.jsonl                 │
│  Gemini ──→ Wrapper Script   ──→ events.jsonl                 │
│                                                                │
│  공유 로그: ~/.orchestrator/events.jsonl (append-only)          │
├──────────────────────────────────────────────────────────────┤
│  Layer 2: Classify (자동 분류)                                  │
│                                                                │
│  파일 경로 패턴 → domain (frontend/backend/database/...)       │
│  명령어 패턴   → lifecycle (discovery/planning/development/...) │
│  에이전트 이름 → purpose (feature/bugfix/refactor/...)          │
│  CLI 이름     → cost bucket (claude/codex/gemini)              │
├──────────────────────────────────────────────────────────────┤
│  Layer 3: Visualize (시각화)                                    │
│                                                                │
│  SvelteKit Dashboard (localhost:4173)                          │
│  ├── SSE 엔드포인트 (fs.watch → 브라우저 push)                   │
│  ├── Pipeline Flow (SVG 노드 + 엣지 + pulse 애니메이션)         │
│  ├── Command History (타임라인 + 접기/펼치기)                    │
│  └── Analytics (cost, tokens, domain coverage, gate progress)  │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 이벤트 스키마

```jsonc
// ~/.orchestrator/events.jsonl — 한 줄 = 한 이벤트
{
  // 식별
  "id": "evt_a1b2c3",
  "pipeline_id": "pipe_x1y2z3",     // 하나의 명령 = 하나의 pipeline
  "ts": "2026-02-27T10:30:00Z",

  // WHO — 누가 실행했나
  "cli": "claude",                    // claude | codex | gemini
  "agent": "api-designer",           // 에이전트명 (없으면 null)
  "team": null,                       // Agent Teams 이름 (없으면 null)
  "teammate": null,                   // 팀원 이름 (없으면 null)

  // WHY — 왜 (원래 의도)
  "intent": "auth API 엔드포인트 설계",
  "purpose": "feature",              // feature | bugfix | refactor | docs | test | infra

  // WHAT — 뭘 했나
  "action": "file_write",            // pipeline_start | pipeline_end |
                                      // file_write | file_edit | command_run |
                                      // agent_spawn | team_create | error
  "target": "src/routes/auth.ts",
  "detail": "POST /api/auth/login 엔드포인트 생성",

  // WHERE — 전주기/기술스택 어디
  "lifecycle": "development",        // discovery | planning | development |
                                      // testing | deployment | maintenance
  "domain": "backend",              // frontend | backend | database | infrastructure |
                                      // security | testing | observability | documentation
  "stage": "mvp",                   // mvp | poc | production

  // COST — 얼마나
  "tokens": { "in": 3200, "out": 1500 },
  "cost_usd": 0.08,
  "duration_ms": 4500,

  // STATUS — 노드 상태
  "status": "done"                   // queued | active | streaming | done | error
}
```

### 4.3 파이프라인 그루핑

하나의 명령 = 하나의 `pipeline_id`:

```jsonc
// 명령 시작
{ "id": "evt_001", "pipeline_id": "pipe_abc", "action": "pipeline_start",
  "intent": "auth 시스템 구현해", "purpose": "feature" }

// 하위 이벤트들
{ "id": "evt_002", "pipeline_id": "pipe_abc", "cli": "claude",
  "agent": "api-designer", "action": "file_write", "target": "src/routes/auth.ts" }

{ "id": "evt_003", "pipeline_id": "pipe_abc", "cli": "codex",
  "action": "file_write", "target": "src/components/Login.tsx" }

// 명령 완료
{ "id": "evt_099", "pipeline_id": "pipe_abc", "action": "pipeline_end",
  "duration_ms": 180000, "cost_usd": 0.45 }
```

### 4.4 자동 분류 규칙

```javascript
// domain 분류 (파일 경로 기반)
const DOMAIN_RULES = {
  frontend:       /\.(tsx|jsx|css|scss|svelte|vue)$|components?\/|pages?\//,
  backend:        /routes?\/|controllers?\/|services?\/|middleware\/|api\//,
  database:       /prisma|migration|schema|seed|\.sql$/,
  infrastructure: /docker|Dockerfile|\.ya?ml$|terraform|k8s|deploy|\.github/,
  security:       /auth|security|permission|rbac|\.env/,
  testing:        /test|spec|__test__|coverage|playwright|vitest/,
  observability:  /monitor|metric|health|alert|sentry|log/,
  documentation:  /\.md$|docs?\//,
};

// lifecycle 분류 (에이전트/스킬 이름 기반)
const LIFECYCLE_RULES = {
  discovery:    /project-discovery|brainstorming|analyze/,
  planning:     /prd-writer|tech-spec|phase-tracker|plan/,
  development:  /scaffolder|api-designer|db-architect|auth-builder|craft/,
  testing:      /test-strategist|security-scanner|lint-configurator|quality-gate/,
  deployment:   /container-builder|ci-generator|craft-deploy/,
  maintenance:  /migration-planner|lifecycle-gate|analytics/,
};
```

### 4.5 대시보드 기술 스택

| 레이어 | 기술 | 이유 |
|--------|------|------|
| UI 프레임워크 | SvelteKit | open-claude-office의 /craft-preview 인프라 재사용 |
| 노드 그래프 | SVG + Svelte transitions | 가벼운 애니메이션, CSS keyframes |
| 실시간 통신 | SSE (Server-Sent Events) | fs.watch → SSE push, 단방향이라 단순 |
| 데이터 저장 | JSONL (append-only) | 서버/DB 불필요, 파일 시스템만으로 동작 |
| 서빙 | SvelteKit dev server (:4173) | WSL → Windows 브라우저 포트 포워딩 |
| 스타일 | Tailwind CSS 4 | open-claude-office/craft 생태계와 통일 |

---

## 5. 이벤트 수집 방법

### 5.1 Claude Code — Hook 기반 (자동)

Claude의 PostToolUse hook이 모든 도구 사용을 자동 기록:

```bash
# ~/.claude/hooks/orchestrator-capture.sh
#!/usr/bin/env bash
TOOL_NAME="$1"
EVENTS_FILE="$HOME/.orchestrator/events.jsonl"
TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
EVENT_ID="evt_$(head -c 6 /dev/urandom | xxd -p)"

mkdir -p "$(dirname "$EVENTS_FILE")"

# stdin에서 도구 입력 읽기
INPUT=$(cat)
FILE_PATH=$(echo "$INPUT" | jq -r '.file_path // .command // ""' 2>/dev/null)

# 이벤트 기록
echo "{\"id\":\"$EVENT_ID\",\"ts\":\"$TIMESTAMP\",\"cli\":\"claude\",\"action\":\"$TOOL_NAME\",\"target\":\"$FILE_PATH\",\"status\":\"done\"}" >> "$EVENTS_FILE"
```

settings.json에 hook 추가:
```json
{
  "hooks": {
    "PostToolUse": [
      { "matcher": "*",
        "hooks": [{"type": "command", "command": "~/.claude/hooks/orchestrator-capture.sh \"$TOOL_USE\""}]
      }
    ]
  }
}
```

### 5.2 Codex CLI — Wrapper 스크립트

```bash
# ~/.orchestrator/wrappers/codex-work.sh
#!/usr/bin/env bash
EVENTS_FILE="$HOME/.orchestrator/events.jsonl"
PIPE_ID="pipe_$(head -c 6 /dev/urandom | xxd -p)"
TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
INTENT="$*"

# pipeline 시작 이벤트
echo "{\"id\":\"evt_$(head -c 6 /dev/urandom | xxd -p)\",\"pipeline_id\":\"$PIPE_ID\",\"ts\":\"$TIMESTAMP\",\"cli\":\"codex\",\"action\":\"pipeline_start\",\"intent\":\"$INTENT\",\"status\":\"active\"}" >> "$EVENTS_FILE"

START_MS=$(date +%s%3N)

# Codex 실행 (stdout/stderr 캡처)
codex "$@" 2>&1 | tee /tmp/codex-output-$PIPE_ID.log

EXIT_CODE=$?
END_MS=$(date +%s%3N)
DURATION=$((END_MS - START_MS))
END_TS=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

# pipeline 종료 이벤트
STATUS="done"
[ $EXIT_CODE -ne 0 ] && STATUS="error"

echo "{\"id\":\"evt_$(head -c 6 /dev/urandom | xxd -p)\",\"pipeline_id\":\"$PIPE_ID\",\"ts\":\"$END_TS\",\"cli\":\"codex\",\"action\":\"pipeline_end\",\"duration_ms\":$DURATION,\"status\":\"$STATUS\"}" >> "$EVENTS_FILE"
```

### 5.3 Gemini CLI — Wrapper 스크립트

Codex wrapper와 동일한 패턴, `codex` → `gemini` 교체.

### 5.4 bashrc 별칭

```bash
# ~/.bashrc에 추가
alias cw='~/.orchestrator/wrappers/codex-work.sh'   # codex-work
alias gw='~/.orchestrator/wrappers/gemini-work.sh'   # gemini-work
# Claude는 hook이 자동 기록하므로 별칭 불필요
```

---

## 6. 향후 통합 시스템 활용 계획

### 6.1 stitch-skills 통합

```
Phase 2 (대시보드 빌드) 시 활용:

1. enhance-prompt 스킬
   사용자의 짧은 명령 → 최적화된 프롬프트로 변환
   "auth 구현해" → 구체적인 기술 요구사항으로 확장
   → 파이프라인 입력 품질 향상

2. react-components 스킬
   대시보드 UI 컴포넌트를 빠르게 생성
   노드 그래프 컴포넌트, 타임라인 컴포넌트 등

3. design-md 스킬
   대시보드의 디자인 시스템(DESIGN.md) 자동 생성
   색상, 타이포그래피, 컴포넌트 명세 일관성 유지

4. stitch-loop 스킬
   단일 프롬프트로 대시보드 전체 페이지 반복 빌드
```

### 6.2 remotion 통합

```
Phase 3 (History & Analytics) 이후 활용:

1. 파이프라인 리플레이 비디오
   events.jsonl의 시간 순서 이벤트를 React 컴포넌트로 렌더링
   → MP4 비디오로 내보내기
   → 개발 과정을 영상으로 회고

2. 스프린트 회고 영상
   스프린트 기간의 모든 파이프라인을 시간 순으로 재생
   어떤 도메인에서 얼마나 활동했는지 시각화

3. 릴리스 데모 영상
   MVP → PoC → Production 전환 과정을 영상으로 기록
   각 게이트 통과 순간을 하이라이트

4. stitch-skills 연동
   stitch-skills에 포함된 remotion 스킬 활용
   프로젝트 워크스루 비디오 자동 생성
```

### 6.3 claude-office-skills 통합

```
Phase 3 (History & Analytics) 이후 활용:

1. PPTX 리포트 자동 생성
   파이프라인 히스토리 → PowerPoint 프레젠테이션
   도메인별 커버리지 차트, 비용 분석, 게이트 진행률

2. XLSX 비용 분석
   CLI별 토큰 사용량, 비용을 Excel 시트로 내보내기
   수식 기반 예측 모델 (월간 비용 추정)

3. DOCX 기술 문서
   각 파이프라인에서 생성된 코드에 대한 기술 문서 자동 생성
   변경 추적 기능으로 리뷰 이력 관리

4. PDF 리포트
   정기 리포트 (주간/스프린트) PDF 자동 생성
   이메일/Slack 자동 배포용

의존성 설치 (Ubuntu):
  sudo apt install libreoffice poppler-utils pandoc
```

---

## 7. 구현 단계

### Phase 1: Event Capture 인프라

**목표**: 3개 CLI의 모든 활동을 하나의 JSONL에 기록

```
작업 항목:
├── [1.1] ~/.orchestrator/ 디렉토리 구조 생성
├── [1.2] events.jsonl 스키마 확정 및 검증 스크립트
├── [1.3] Claude PostToolUse hook (orchestrator-capture.sh)
├── [1.4] Codex wrapper 스크립트 (codex-work.sh)
├── [1.5] Gemini wrapper 스크립트 (gemini-work.sh)
├── [1.6] bashrc 별칭 등록
├── [1.7] 파이프라인 그루핑 (pipeline_id 생성 로직)
└── [1.8] 자동 분류기 (domain/lifecycle 규칙)

담당: Claude (hook 설정) + Codex (wrapper 스크립트 탐색/테스트)
예상: 1세션
```

### Phase 2: SvelteKit Pipeline Dashboard

**목표**: 실시간 파이프라인 시각화

```
작업 항목:
├── [2.1] SvelteKit 프로젝트 초기화 (open-claude-office/dashboard/)
├── [2.2] SSE 엔드포인트 (fs.watch → events.jsonl → 브라우저 push)
├── [2.3] 파이프라인 노드 컴포넌트 (SVG + pulse 애니메이션)
├── [2.4] 엣지 컴포넌트 (flow 애니메이션)
├── [2.5] 파이프라인 레이아웃 엔진 (자동 노드 배치)
├── [2.6] CLI 출력 실시간 스트리밍 (노드 클릭 시)
├── [2.7] /craft-preview 연동 (localhost:4173)
└── [2.8] stitch-skills 활용 (react-components, design-md)

담당: Claude (아키텍처, 핵심 로직) + Codex (컴포넌트 코드 생성)
예상: 2-3세션
```

### Phase 3: History & Analytics

**목표**: 기록 뷰 + 분석 + 리포트 생성

```
작업 항목:
├── [3.1] Command History 타임라인 뷰 (접기/펼치기)
├── [3.2] Domain × Lifecycle 히트맵
├── [3.3] CLI별 토큰/비용 트래커
├── [3.4] MVP Gate 자동 체크 (lifecycle-gate 연동)
├── [3.5] Context 사용률 모니터 (각 CLI별)
├── [3.6] remotion 연동 (파이프라인 리플레이 비디오)
├── [3.7] claude-office-skills 연동 (PPTX/XLSX/PDF 리포트)
└── [3.8] /remote-control 연동 (폰에서 모니터링)

담당: Claude (분석 로직) + Gemini (검증) + Codex (탐색/코드생성)
예상: 2-3세션
```

### Phase 4: 최적화 & 확장

**목표**: 토큰 최적화, 전주기 자동화

```
작업 항목:
├── [4.1] 토큰 최적화 알고리즘 (CLI별 최적 배분)
├── [4.2] 컨텍스트 80% 도달 시 자동 요약/세션 교체 알림
├── [4.3] open-claude-office context-optimizer 연동
├── [4.4] 자동 CLI 라우팅 (작업 유형 → 최적 CLI 추천)
├── [4.5] Agent Teams 자동 구성 (복잡도 기반)
├── [4.6] stitch-skills enhance-prompt 연동 (입력 최적화)
└── [4.7] 스프린트/릴리스 자동 리포트 생성 파이프라인

담당: Claude (최적화 로직) + 전체 CLI 협업
예상: 2-3세션
```

---

## 8. 전주기 데이터 흐름도

하나의 명령이 전주기와 기술 스택을 거쳐 흐르는 전체 그림:

```
[사용자 명령]
  "auth 시스템 구현해"
       │
       ▼
┌──────────────┐
│ 의도 분류     │  purpose: feature
│ 도메인 추론   │  domain: backend + frontend + security
│ 단계 확인     │  stage: mvp
│ CLI 배분     │  claude: 설계, codex: 생성, gemini: 검증
└──────┬───────┘
       │
       ├────────────────────────────────────────────┐
       │                                            │
  ┌────▼────┐  ┌─────────┐  ┌─────────┐     ┌─────▼─────┐
  │ Claude  │  │ Claude  │  │ Codex   │     │ Gemini    │
  │ api-    │  │ db-     │  │ (코드   │     │ (테스트   │
  │ designer│  │ architect│ │  생성)  │     │  검증)    │
  └────┬────┘  └────┬────┘  └────┬────┘     └─────┬─────┘
       │            │            │                 │
       ▼            ▼            ▼                 ▼
  [auth.ts]    [schema.prisma] [Login.tsx]    [auth.test.ts]
       │            │            │                 │
       │            │            │                 │
  ┌────▼────────────▼────────────▼─────────────────▼─────┐
  │                                                        │
  │  전주기 매핑                   기술 스택 매핑             │
  │  ├── discovery    ░░          ├── frontend    ██       │
  │  ├── planning     ░░          ├── backend     ████     │
  │  ├── development  ████  ←     ├── database    ██       │
  │  ├── testing      ██          ├── security    ██       │
  │  ├── deployment   ░░          ├── infra       ░░       │
  │  └── maintenance  ░░          └── observ.     ░░       │
  │                                                        │
  │  MVP Gate: 3/4 passed                                  │
  │  Token: Claude 5.2k + Codex 2.1k + Gemini 0.8k        │
  │  Cost: $0.45                                           │
  │                                                        │
  └────────────────────────────────────────────────────────┘
       │
       ▼
  [events.jsonl에 기록 → Dashboard에 실시간 반영]
  [History에 파이프라인 단위로 정리]
```

---

## 9. 파일 구조 (현재)

```
open-claude-office/
├── CLAUDE.md                          ← AI agent system prompt
├── README.md                          ← Public documentation
├── install.sh                         ← Full installation script
├── .claude/                           ← Agent ecosystem (47 agents)
│   ├── settings.json                  ← Unified configuration
│   ├── agents/                        ← 47 agents + MANIFEST.md
│   ├── skills/                        ← 28 skills
│   ├── commands/                      ← 14 commands
│   ├── hooks/                         ← 9 hooks
│   ├── docs/                          ← ROLE-MAP.md, ARCHITECTURE.md
│   ├── templates/                     ← Phase templates
│   ├── analytics/                     ← Metric data
│   ├── logs/                          ← Hook logs
│   └── scripts/                       ← Utility scripts
├── modules/                           ← 8 install modules
│   ├── core/manifest.json
│   ├── craft/                         ← + assets/ (design, templates, workspace)
│   ├── lifecycle/manifest.json
│   ├── git/manifest.json
│   ├── quality/manifest.json
│   ├── analytics/manifest.json
│   ├── multi-cli/manifest.json
│   └── meta/manifest.json
├── dashboard/                         ← SvelteKit Pipeline Dashboard
│   ├── src/
│   │   ├── routes/                    ← Pages + API endpoints
│   │   │   ├── +layout.svelte        ← VSCode-like layout
│   │   │   ├── +page.svelte          ← Pipeline Flow (main)
│   │   │   └── api/
│   │   │       ├── projects/          ← ~/dev/ folder scanning
│   │   │       └── events/            ← SSE stream
│   │   └── lib/
│   │       ├── components/            ← UI components
│   │       │   ├── Sidebar.svelte     ← Project explorer
│   │       │   ├── TopBar.svelte      ← Tabs + lifecycle toggle
│   │       │   ├── PipelineNode.svelte
│   │       │   ├── PipelineGraph.svelte
│   │       │   └── StatusBar.svelte
│   │       ├── stores/                ← Svelte stores
│   │       └── types.ts              ← TypeScript types
│   └── package.json
├── docs/                              ← Project documentation
│   ├── ORCHESTRATOR-PIPELINE-PLAN.md  ← This document
│   ├── DEVELOPMENT-LIFECYCLE.md
│   ├── 개발전주기.md
│   ├── MVP-POC개발전주기.md
│   └── archive/
└── .orchestrator/                     ← Event capture (→ ~/.orchestrator)
    ├── events.jsonl                   ← Shared event log
    └── wrappers/
```

---

## 10. 토큰/비용 최적화 전략

### 10.1 CLI별 최적 배분

```
작업 유형별 라우팅:

탐색/조사 (토큰 소모 큼)  →  Codex (저비용)
  파일 검색, 코드 분석, 패턴 탐색, 의존성 조사

코드 생성 (토큰 소모 중간)  →  Codex (코드 특화) 또는 Claude (복잡한 경우)
  컴포넌트 생성, 리팩토링, 보일러플레이트

설계/아키텍처 (정확도 중요)  →  Claude (최고 품질)
  API 설계, 시스템 아키텍처, 복잡한 로직

검증/분석 (대용량 입력)  →  Gemini (대용량 컨텍스트)
  코드 리뷰, 보안 스캔, 테스트 결과 분석

오케스트레이션 (Agent Teams)  →  Claude (Teams 기능)
  멀티 에이전트 조율, 복잡한 협업 작업
```

### 10.2 컨텍스트 최적화

```
컨텍스트 사용률 모니터링:
├── StatusLine에 실시간 표시 (이미 구현됨)
├── 80% 도달 시 ⚠️ 경고
├── 90% 도달 시 자동 요약 제안
└── open-claude-office context-optimizer 연동
    ├── incremental loading 전략
    ├── token budget 관리 (quick/standard/deep/full)
    └── context boundary 자동 조절
```

### 10.3 비용 추적

대시보드의 CostTracker가 실시간으로:
- CLI별 누적 비용 표시
- 도메인별 비용 분배
- 세션 비용 vs 일간/주간 추세
- 예산 초과 알림

---

## 부록 A: 참조 프로젝트 비교

| | OpenClaw | Claw Empire | Claude Code | **이 프로젝트** |
|--|---------|-------------|-------------|---------------|
| **핵심** | Gateway + Plugin | CEO 회사 시뮬레이션 | 3-Layer Agent | Interactive Pipeline |
| **시각화** | 없음 (CLI) | PixiJS 픽셀아트 | StatusLine (터미널) | SVG 노드 그래프 |
| **CLI 통합** | 단일 (LLM API) | 6종 CLI spawn | Claude 단독 | Claude+Codex+Gemini |
| **상태 추적** | 세션 기반 | SQLite 태스크 머신 | Task List | JSONL append-only |
| **격리** | Session scope | Git worktree | worktree (선택) | Wrapper 스크립트 |
| **비용 추적** | 없음 | 없음 | /cost 명령 | 실시간 대시보드 |

## 부록 B: 관련 문서

| 문서 | 위치 | 내용 |
|------|------|------|
| DEVELOPMENT-LIFECYCLE.md | docs/ | MVP→PoC→Prod 3단계 프로세스 |
| MVP-POC개발전주기.md | docs/ | 기술 분야별 단계 체크리스트 |
| 개발전주기.md | docs/ | 8개 기술 도메인 상세 |
| CLAUDE.md | 루트 | AI agent system prompt (47 agents, 28 skills) |
| .claude/settings.json | .claude/ | Unified configuration (hooks, lifecycle, quality-gate) |
| .claude/agents/MANIFEST.md | .claude/agents/ | 47개 에이전트 키워드 라우팅 인덱스 |
| .claude/docs/ROLE-MAP.md | .claude/docs/ | 모듈별 역할 매핑 |
| .claude/docs/ARCHITECTURE.md | .claude/docs/ | 시스템 아키텍처 |
